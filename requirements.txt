torch>=2.7
numpy
regex
fastapi>=0.110
uvicorn[standard]
pydantic>=2
httpx
requests
matplotlib
pytest

# Optional — only needed for scripts/fetch_wikipedia_hf.py (corpus enrichment):
#   pip install datasets
